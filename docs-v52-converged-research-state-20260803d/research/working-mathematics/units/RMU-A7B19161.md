---
title: "Filtered one-layer descent"
description: "Let \\(D:U\\to V\\) be a linear map of finite-dimensional vector spaces and\nlet \\(V_{\\le d}\\subseteq V\\) be a filtered subspace.  For\n\\(\\Phi\\in V\\), the following are equivalent:\n\\begin{enumerate}[label=(\\roman*)]\n\\item there exists \\(u\\in U\\) such that\n\\(\\Phi-Du\\in V_{\\le d}\\);\n\\item every\n\\[\n\\lambda\\in(\\operatorname{im}D)^\\perp\n\\cap(V_{\\le d})^\\perp\n\\]\nsatisfies \\(\\lambda(\\Phi)=0\\).\n\\end{enumerate}"
---

# Filtered one-layer descent

`RMU-A7B19161` · `lemma` · statement version `1`

## Exact statement

Let \(D:U\to V\) be a linear map of finite-dimensional vector spaces and
let \(V_{\le d}\subseteq V\) be a filtered subspace.  For
\(\Phi\in V\), the following are equivalent:
\begin{enumerate}[label=(\roman*)]
\item there exists \(u\in U\) such that
\(\Phi-Du\in V_{\le d}\);
\item every
\[
\lambda\in(\operatorname{im}D)^\perp
\cap(V_{\le d})^\perp
\]
satisfies \(\lambda(\Phi)=0\).
\end{enumerate}

## Evidence and source access

### A proof body follows this labelled manuscript statement.

`SUP-RMUA7B19161-01` · `proof`

A proof body follows this labelled manuscript statement.

**Establishes:** Supplies the manuscript proof attached to this statement.

**Source:** Inline evidence is reproduced below.

Let \(q:V\to V/\operatorname{im}D\).  Condition (i) says
\(q(\Phi)\in q(V_{\le d})\).  Finite-dimensional duality says precisely
that every functional on the quotient vanishing on \(q(V_{\le d})\) must
also vanish on \(q(\Phi)\).  Pulling the functionals back to \(V\) gives
(ii).

[Machine-readable graph](../graph.json)
