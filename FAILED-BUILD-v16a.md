# Failed v16a validation

The write-once `docs-v16a-20260729a/` tree rendered successfully and passed
the deterministic generation check. Public-site validation then found that
the Program 2 source contained 4,108 words, 108 words beyond the validator's
inclusive 2,000--4,000 expectation.

The mathematical scope was not changed. Repeated replay-history prose was
condensed in additive source release `model-briefs-v7a-20260729a`, and the
next generated candidate is `docs-v16b-20260729a/`. This v16a tree remains
unselected construction history.
