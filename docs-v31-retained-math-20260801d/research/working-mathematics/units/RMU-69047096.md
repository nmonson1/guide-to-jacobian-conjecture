# One-wall control

`RMU-69047096` · `theorem`

This page is generated from the retained mathematical graph.

## Mathematical record

`RMU-69047096` · `theorem`

Inside the generic polynomial algebra,
\[
A_N^{K_N}
=
A_N\cap (A_N^{K_N})_{(q_N)}.
\]
Thus simultaneous escapes impose no additional divisorial conditions on
global invariant functions.

Support:

- **proof:** A proof body follows this labelled manuscript statement. — Inline support is reproduced on the unit record.

The differential sequence has free middle terms, and its cokernel is
supported on \(q_N=0\).  The resulting homogeneous invariant module is the
intersection of the free module with its localization at that one
height-one prime.  This Cohen--Macaulay support argument replaces an earlier
claim that all homogeneous invariant modules were reflexive; that stronger
claim was unnecessary and is not used here.

  - Does not establish: Presence in the manuscript is not an independent proof audit.
